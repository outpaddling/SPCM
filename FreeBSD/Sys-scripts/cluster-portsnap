#!/bin/sh -e

portsnap fetch && portsnap update
for node in `cluster-compute-nodes`; do
    if ! rsync -av --delete /usr/ports ${node}:/usr; then
	printf "$node\n" >> cluster-portsnap-failed-nodes
    fi
done

